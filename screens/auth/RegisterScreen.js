import React from 'react'
import Register from '../../components/LoginSignup/Signin'

const RegisterScreen = ({ navigation }) => {
    return (
        <Register navigation={navigation} />
    )
}

export default RegisterScreen